<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/confirm_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="16dp"
        android:text="@string/close_referral"
        android:textColor="@color/primary_color"
        android:fontFamily="@font/google_sans_bold"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/confirm_title"
        app:layout_constraintLeft_toLeftOf="@id/confirm_title"
        android:layout_marginTop="16dp"
        android:layout_marginRight="16dp"
        android:text="Are you sure you want to mark this referral as complete?"
        android:fontFamily="@font/google_sans_regular"/>

</androidx.constraintlayout.widget.ConstraintLayout>